
set up github with readme.txt? {
    run in succession:
    1. git branch --set-upstream-to=origin/master master
    2. git pull
    3. git pull origin master --allow-unrelated-histories
    4. git push orign master
}
